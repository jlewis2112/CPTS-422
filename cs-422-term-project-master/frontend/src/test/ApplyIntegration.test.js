import { ApplyTest } from './ApplyTest'

// Before all tests
beforeAll(async () => {
});

// After each test
afterEach(async () => { 
});

// After all the tests
afterAll(async () => { 
});


// Close your eyes. You didn't see this.
test(ApplyTest[0].name, ApplyTest[0].test);
test(ApplyTest[1].name, ApplyTest[1].test);
test(ApplyTest[2].name, ApplyTest[2].test);
